# ESP32 RGBW LED Strip Control Project

## 项目概述
本项目旨在通过移动应用控制ESP32开发板外接的RGBW灯带，实现颜色编辑、模式切换等功能。

## 系统架构

### 整体架构
```
+-------------+     +-------------+     +-------------+
| 移动应用APP | <--> | ESP32开发板 | <--> | RGBW灯带   |
+-------------+     +-------------+     +-------------+
```

### 硬件架构
- **ESP32开发板**：作为控制中心，运行嵌入式代码
- **RGBW灯带**：可编辑的LED灯带，支持RGBW四通道控制
- **电源**：为ESP32和灯带提供电源
- **连接方式**：使用GPIO引脚控制灯带

### 软件架构
1. **嵌入式程序**（ESP32）
   - WiFi连接模块
   - 灯带控制模块
   - 通信服务器模块
   - 数据处理模块

2. **移动应用**
   - 用户界面
   - 通信模块
   - 颜色选择器
   - 模式管理

## 硬件连接方案

### 硬件清单
- ESP32开发板（如ESP32-DevKitC）
- RGBW LED灯带（支持PWM控制）
- 面包板或PCB
- 连接线
- 电源适配器（5V，根据灯带长度选择合适电流）

### 连接图
```
ESP32        RGBW灯带
+---+        +---+
|   |        |   |
| 5V|--------| V+|
| GND|-------| GND|
| GPIO23|----| R  |
| GPIO22|----| G  |
| GPIO21|----| B  |
| GPIO19|----| W  |
+---+        +---+
```

## 软件设计

### 嵌入式程序（ESP32）

#### 目录结构
```
esp32-rgbw-control/
├── platformio.ini          # PlatformIO配置文件
└── src/
    ├── main.cpp            # 主程序
    ├── wifi.cpp            # WiFi连接管理
    ├── wifi.h
    ├── led_control.cpp     # 灯带控制
    ├── led_control.h
    ├── server.cpp          # Web服务器
    ├── server.h
    └── config.h            # 配置文件
```

#### 核心功能
1. **WiFi连接**：配置为AP模式或Station模式
2. **灯带控制**：PWM调节RGBW通道
3. **Web服务器**：提供HTTP API接口
4. **数据处理**：解析APP发送的命令

### 移动应用

#### 技术选型
- **框架**：Flutter（跨平台支持）
- **通信**：HTTP请求
- **UI组件**：颜色选择器、模式选择器

#### 核心功能
1. **连接管理**：扫描并连接ESP32
2. **颜色编辑**：RGBW颜色选择
3. **模式控制**：预设模式切换
4. **亮度调节**：整体亮度控制

## 通信协议

### 数据格式（JSON）
```json
{
  "command": "set_color",
  "data": {
    "r": 255,
    "g": 0,
    "b": 0,
    "w": 0,
    "brightness": 100
  }
}
```

### 命令类型
- `set_color`：设置颜色
- `set_mode`：设置模式（如呼吸、渐变、闪烁等）
- `set_brightness`：设置亮度
- `get_status`：获取当前状态

## 工作流程

1. **初始化**
   - ESP32启动并初始化WiFi
   - 移动应用扫描并连接ESP32

2. **控制流程**
   - 用户在APP中选择颜色或模式
   - APP发送命令到ESP32
   - ESP32接收并解析命令
   - ESP32控制灯带执行相应操作
   - ESP32返回执行结果

3. **异常处理**
   - 连接断开重连
   - 命令执行失败处理
   - 电源异常处理

## 开发环境

### 嵌入式开发
- **IDE**：Visual Studio Code + PlatformIO插件
- **框架**：Arduino框架
- **依赖库**：WiFi.h, ESPAsyncWebServer.h, FastLED.h

### 移动应用开发
- **IDE**：Android Studio / VS Code
- **框架**：Flutter
- **依赖**：http, flutter_colorpicker

## 测试计划

1. **硬件测试**
   - 电源稳定性测试
   - 灯带响应测试

2. **软件测试**
   - WiFi连接测试
   - 命令执行测试
   - 模式切换测试

3. **集成测试**
   - APP与ESP32通信测试
   - 系统稳定性测试

## 部署步骤

1. **硬件部署**
   - 连接ESP32与RGBW灯带
   - 连接电源

2. **软件部署**
   - 使用PlatformIO上传ESP32代码
   - 安装移动应用

3. **配置**
   - 连接ESP32 WiFi网络
   - 配置APP连接参数

## 扩展功能

- **语音控制**：集成语音助手
- **定时任务**：设置开关时间
- **场景模式**：预设多种场景灯光
- **多设备控制**：控制多个ESP32设备

## 注意事项

1. **电源安全**：确保电源适配器电流足够驱动灯带
2. **WiFi稳定性**：确保ESP32与APP之间的WiFi连接稳定
3. **散热**：ESP32长时间运行时注意散热
4. **兼容性**：确保RGBW灯带与ESP32的PWM控制兼容

## 项目进度

- [ ] 硬件连接设计
- [ ] 嵌入式程序开发
- [ ] 移动应用开发
- [ ] 系统集成测试
- [ ] 文档完善
