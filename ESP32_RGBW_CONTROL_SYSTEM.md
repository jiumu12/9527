# ESP32 RGBW 灯带控制系统

## 项目概述

基于 ESP32 的智能 RGBW 灯带控制系统，支持通过移动应用和 Web 界面进行控制，具有丰富的灯光效果和配置选项。系统采用非易失性存储配置，无需硬编码 WiFi 凭证，提供友好的配网功能。

## 系统架构

### 硬件层
- **主控芯片**：ESP32 系列（兼容 ESP32-S3, -C3 等）
- **灯带驱动**：支持 SK6812、WS2812B+W 等 RGBW 灯带
- **电源**：外部 5V/2A 以上电源适配器

### 固件层
- **硬件抽象层**：隔离具体硬件操作
- **网络与服务层**：Wi-Fi 连接、mDNS 服务广播、WebSocket/TCP 服务器
- **通信协议层**：JSON 协议（支持二进制协议扩展）
- **数据处理与引擎层**：核心逻辑，解析指令，管理灯光状态
  - 使用状态机设计模式管理灯光效果，提高代码可维护性
  - 使用 FreeRTOS 进行任务调度，分离网络处理和灯光控制任务
  - 实现更完善的错误处理机制，提高系统稳定性
- **灯带驱动抽象层**：统一不同灯带芯片的驱动接口
- **配置管理**：使用 Preferences 库进行非易失性存储
- **内存管理**：实现内存池管理，减少内存碎片化，添加内存使用监控

### 客户端层
- **网络发现层**：自动扫描局域网内的设备
- **通信层**：与 ESP32 建立 WebSocket 连接
- **UI 呈现层**：提供控制和配置界面
- **业务逻辑层**：处理用户输入，生成控制指令

## 主要功能

### 灯光控制
- **静态颜色**：精确控制 RGBW 四个通道的亮度
- **动态效果**：呼吸、彩虹、闪烁等效果
- **亮度调节**：全局亮度控制（0-100%）
- **速度调节**：动态效果速度控制

### 配置管理
- **Wi-Fi 配置**：支持 AP 模式和 Station 模式，无需硬编码
- **LED 配置**：可调整 LED 引脚、数量和最大亮度
- **Web 配置界面**：通过浏览器访问 http://esp32-rgbw.local/config
- **移动应用配置**：通过 Flutter 应用进行配置
- **配置持久化**：使用 Preferences 库存储配置，重启后保持

### 网络功能
- **mDNS 服务发现**：设备自动广播服务，便于应用发现
- **WebSocket 通信**：实时双向通信
- **网络状态监控**：自动重连和故障恢复
- **智能回退**：Station 模式连接失败时自动回退到 AP 模式

## 硬件连接

### 接线说明
1. **LED 灯带**：
   - DI 引脚 → ESP32 GPIO7（可配置）
   - 5V → 外部电源正极
   - GND → 外部电源负极和 ESP32 GND

2. **电源**：
   - 使用 5V/2A 以上的电源适配器
   - 确保 ESP32 和灯带共地

3. **建议**：
   - 在数据线上串联 220-470Ω 电阻
   - 在 VCC 与 GND 间并联 100-1000uF 电容

## 软件安装

### 固件编译与上传
1. **环境搭建**：
   - 安装 PlatformIO
   - 克隆项目代码

2. **编译上传**：
   ```bash
   cd esp32-rgbw-control
   platformio run --target upload
   ```

### 移动应用安装
1. **环境搭建**：
   - 安装 Flutter SDK
   - 克隆项目代码

2. **依赖安装**：
   ```bash
   cd esp32-rgbw-app
   flutter pub get
   ```

3. **运行应用**：
   ```bash
   flutter run
   ```

## 配网流程

### 首次配网
1. **设备启动**：设备默认以 AP 模式启动，SSID 为 "ESP32-RGBW"，密码为 "12345678"
2. **连接设备**：使用手机或电脑连接到设备的 Wi-Fi 网络
3. **访问配置页面**：打开浏览器，访问 http://esp32-rgbw.local/config
4. **填写配置**：选择 "Station 模式"，输入家庭 Wi-Fi 的 SSID 和密码
5. **保存配置**：点击 "保存配置" 按钮，设备会自动重启并连接到家庭 Wi-Fi

### 重新配置
1. **方法一**：如果设备已连接到家庭 Wi-Fi，直接在局域网内访问 http://esp32-rgbw.local/config
2. **方法二**：如果无法连接到设备，长按设备复位按钮（如果有）或重新烧录固件，设备会恢复到 AP 模式

### 移动应用配网
1. **打开应用**：启动 Flutter 应用
2. **发现设备**：应用会自动扫描局域网内的设备
3. **选择设备**：点击发现的设备进入控制页面
4. **进入配置**：在控制页面点击 "配置" 按钮
5. **修改配置**：修改配置参数并保存
6. **重启应用**：设备重启后，重新连接到设备

## 通信协议

### JSON 协议
```json
// 静态颜色
{
  "cmd": "static",
  "color": {"r": 255, "g": 0, "b": 0, "w": 0},
  "brightness": 80
}

// 动态效果
{
  "cmd": "effect",
  "name": "breath",
  "speed": 50,
  "color": {"r": 255, "g": 0, "b": 0, "w": 0},
  "brightness": 80
}

// 开关控制
{
  "cmd": "switch",
  "on": true
}

// 状态查询
{
  "cmd": "get_status"
}

// 状态响应
{
  "cmd": "status",
  "on": true,
  "mode": 0,
  "color": {"r": 255, "g": 0, "b": 0, "w": 0},
  "brightness": 80,
  "speed": 50
}
```

### 配置 API
- **GET /get-config**：获取当前配置
- **POST /save-config**：保存新配置
- **POST /reset-config**：恢复默认配置

## 故障排除

### 常见问题
1. **设备无法发现**：
   - 检查 Wi-Fi 连接
   - 确保设备已通电
   - 尝试重启设备
   - 如果是首次配网，确保连接到设备的 AP 网络

2. **灯带不亮**：
   - 检查接线是否正确
   - 检查电源是否充足
   - 检查 LED 引脚配置是否正确
   - 检查 LED 数量配置是否与实际一致

3. **配置保存失败**：
   - 检查网络连接
   - 确保配置参数在有效范围内
   - 尝试恢复默认配置

4. **Wi-Fi 连接失败**：
   - 检查 Wi-Fi SSID 和密码是否正确
   - 确保路由器信号强度足够
   - 设备会自动回退到 AP 模式，可重新配置

### 日志查看
通过串口查看设备日志，波特率：115200

## 内存管理

### 内存池管理
- **内存池实现**：创建了固定大小的内存池，用于管理频繁分配和释放的内存
- **内存块管理**：实现了内存块的分配、释放和合并，减少内存碎片化
- **动态内存回退**：当内存池满时，自动回退到动态内存分配

### 内存使用优化
- **静态JSON文档**：使用 `StaticJsonDocument` 替代 `DynamicJsonDocument`，减少动态内存分配
- **减少字符串操作**：优化字符串处理，减少临时字符串的创建
- **内存使用监控**：定期打印内存使用情况，及时发现内存泄漏

### 内存使用监控
- **内存使用统计**：实时监控内存池和堆内存的使用情况
- **内存泄漏检测**：通过定期检查内存使用情况，及时发现内存泄漏
- **内存使用日志**：每5秒打印一次内存使用情况，便于调试和监控

## 灯光效果优化

### 颜色空间转换优化
- **HSV查找表**：预计算HSV到RGBW的转换结果，提高实时效果的处理速度
- **整数运算优化**：使用整数运算替代浮点数运算，减少计算时间
- **RGBW转换算法**：优化RGB到RGBW的转换，利用白色通道提高亮度和能效

### 效果缓存
- **静态效果缓存**：缓存静态颜色效果，避免重复计算
- **状态跟踪**：跟踪上次使用的效果参数，只在参数变化时更新效果
- **缓存有效性检查**：确保缓存数据的有效性，避免使用过期数据

### 硬件加速
- **FPU加速**：使用ESP32的硬件浮点单元加速计算
- **任务调度优化**：减少任务延迟，提高响应速度
- **FastLED优化**：利用FastLED库的硬件加速功能

## 消息队列

### 消息队列实现
- **基于FreeRTOS队列**：使用FreeRTOS的队列机制实现消息队列
- **消息类型**：支持命令消息、状态消息和错误消息
- **队列长度**：最大队列长度为10，确保命令不会丢失

### 消息处理流程
- **消息发送**：WebSocket接收到的命令先发送到消息队列
- **消息处理**：网络任务定期处理消息队列中的消息
- **内存管理**：消息数据使用内存池管理，减少内存碎片化

### 优势
- **命令不丢失**：即使在系统繁忙时，命令也会被存储在队列中等待处理
- **系统稳定性**：消息队列提供了缓冲区，避免系统因突发命令而崩溃
- **并发处理**：支持多客户端同时发送命令，消息队列会有序处理

### 消息类型
- **命令消息**：来自客户端的控制命令，如设置颜色、切换效果等
- **状态消息**：系统状态更新消息，如网络连接状态变化
- **错误消息**：系统错误消息，如配置错误、网络错误等

## 扩展与定制

### 灯光效果扩展
在 `light_engine.cpp` 中添加新的效果函数

### 通信协议扩展
在 `cmd_handler.cpp` 中添加新的命令处理逻辑

### 硬件扩展
通过修改配置文件支持不同的 LED 灯带类型

## 安全考虑

- **Wi-Fi 密码**：存储在设备的非易失性存储器中，加密存储
- **网络访问**：仅在局域网内提供服务，建议使用强密码保护
- **固件更新**：支持通过 OTA 进行固件更新（未来版本）

## 版本历史

- **v1.0**：初始版本，支持基本的灯光控制和配置功能
- **v1.1**：添加配置系统，支持 Web 和移动应用配置
- **v1.2**：优化网络连接和故障恢复机制
- **v1.3**：实现完整的配网功能，支持非易失性配置存储
- **v1.4**：架构优化，引入状态机设计模式，使用 FreeRTOS 任务调度，完善错误处理机制
- **v1.5**：内存管理优化，实现内存池管理，减少内存碎片化，添加内存使用监控
- **v1.6**：实现消息队列，确保命令不丢失，提高系统稳定性
- **v1.7**：灯光效果优化，包括颜色空间转换算法优化、效果缓存和硬件加速

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request，共同改进这个项目。